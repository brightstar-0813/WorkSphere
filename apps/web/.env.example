# Leave empty for local Vite (uses proxy to localhost:4000).
# In production (Vercel), set to your Render API origin, no trailing slash:
# VITE_API_URL=https://worksphere-api.onrender.com
VITE_API_URL=
